/* 畫布物件（文字方塊、自由節點、連線、圈選）的 id 產生器。
 *
 * 以前一律用 'txt_' + Date.now()：同一毫秒內產生兩個（連點、一次複製多個）
 * 就會撞號，之後刪一個會兩個一起消失、拖一個兩個一起動。後面補一段遞增
 * 計數器，同一毫秒內也不會重複。前綴維持原樣，舊存檔的 id 不受影響。 */
let seq = 0;
export const newId = (prefix) => `${prefix}${Date.now()}_${(seq++).toString(36)}`;
